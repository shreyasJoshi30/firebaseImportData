{
  "name": "firebaseimportnode",
  "version": "1.0.0",
  "description": "the project imports a valid JSON file to firebase database by creating collection and its documents iteratively. ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^8.10.0"
  }
}
